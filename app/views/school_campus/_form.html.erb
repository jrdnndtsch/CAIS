<%= form_for(@school_campu) do |f| %>
  <% if @school_campu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school_campu.errors.count, "error") %> prohibited this school_campu from being saved:</h2>

      <ul>
      <% @school_campu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="block">
     <div class="field">
       <%= f.label :campu_id %><br>
       <%= f.collection_select :campu_id, @campus, :id, :feature %>
     </div>
    </div>  
   <div class="actions">
     <%= f.submit %>
     <%= hidden_field_tag :school_id, @school.id %>
   </div>
  
<% end %>
