<%= form_for(@school_academic) do |f| %>
  <% if @school_academic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school_academic.errors.count, "error") %> prohibited this school_academic from being saved:</h2>

      <ul>
      <% @school_academic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
   <%= f.label :academic_id %><br>
   <%= f.collection_select :academic_id, @academics, :id, :course %>

 </div>
 <div class="actions">
   <%= f.submit %>
   <%= hidden_field_tag :school_id, @school.id %>
 </div>
<% end %>
