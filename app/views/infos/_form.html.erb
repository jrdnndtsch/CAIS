<%= form_for(@info) do |f| %>
  <% if @info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@info.errors.count, "error") %> prohibited this info from being saved:</h2>

      <ul>
      <% @info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="block">
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
    <div class="field">
      <%= f.label :department_director %><br>
      <%= f.text_field :department_director %>
    </div>
    <div class="field">
      <%= f.label :percentage_at_college %><br>
      <%= f.text_field :percentage_at_college %>
    </div>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
