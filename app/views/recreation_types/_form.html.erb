<%= form_for(@recreation_type) do |f| %>
  <% if @recreation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recreation_type.errors.count, "error") %> prohibited this recreations_type from being saved:</h2>

      <ul>
      <% @recreation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions recreationtype_submit">
    <%= f.submit %>
  </div>
<% end %>
